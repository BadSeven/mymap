<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunxi.map.dao.UserMapper" >

    <select id="userLogin" resultType="mUser" parameterType="mUser">
          SELECT * FROM muser WHERE uname=#{uname} and pass=md5(#{pass});
    </select>
    <insert id="addUser" parameterType="mUser">
          INSERT  INTO muser (uname, pass, role, phone, email, addtime)  VALUE (#{uname}, md5(#{pass}),#{role},#{phone},#{email}, now())
    </insert>
    <select id="isExist" parameterType="String" resultType="int">
          select COUNT(*) from muser WHERE uname=#{username};
    </select>

</mapper>